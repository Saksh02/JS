#include <bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
    int a;
    int b;
        cout << "Give any two numbers: " << endl;
    cin >> a; b;
 
    int product;
    int sum;
        product = (a * a) + (2*a*b) + (b * b);
        sum = a + b;
            cout << product << endl;
            cout << sum << endl;
            
    return 0;
}
